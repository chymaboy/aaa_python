Функция `elephant` просит купить слона, пока не получит ответ `yes`.

Запуск из командной строки:

`python elephant/my_elephant.py elephants 7` - 7 раз спросит купить слона

Тестирование:

`python -m pytest`